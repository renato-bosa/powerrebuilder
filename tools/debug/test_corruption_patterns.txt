// Test file to analyze corruption patterns

// Case 1: asterisk after closing quote
COLUMN(NAME="address.address_id"*)

// Case 2: asterisk in middle of word inside quotes
COLUMN(NAME="a*dress.update_operator")

// Case 3: asterisk in middle of word not in quotes
WHERE(EXP1="test" OP="=" EXP2="value" LOG*C="and")

// Case 4: asterisk in middle of word inside quotes
JOIN(LEFT="trea*ment_bill.treatment_id" OP="=" RIGHT="treatment.treatment_id")

// Additional test cases
TABLE(NAME="clinic_a*ddress")
COLUMN(NAME="person_address.* ddress_id")
COL*LMN(NAME="test")
LOG*C="and"